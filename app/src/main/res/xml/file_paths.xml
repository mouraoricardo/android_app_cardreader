<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External storage Downloads directory (for sharing exported CSV files) -->
    <external-path
        name="external_downloads"
        path="Download/" />

    <!-- External files directory (for app-specific external storage) -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Cache directory (for temporary files) -->
    <cache-path
        name="cache"
        path="." />
</paths>
